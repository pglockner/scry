# Optional: audio playback with a progress line, `scry -f` window
# with album art, and ogg/opus. Without it, scry falls back to afplay.
# Install with: make deps-audio   (or: brew bundle --file=Brewfile.audio)
brew "mpv"
