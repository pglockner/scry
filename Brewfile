# Core helpers: markdown, images, and syntax-highlighted text.
# Install with: make deps   (or: brew bundle --file=Brewfile)
brew "bat"   # text/code/yaml, and the fallback viewer
brew "glow"  # markdown
brew "viu"   # images outside iTerm2
